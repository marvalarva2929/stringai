node_modules/
.expo/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
.env
.DS_Store

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
# Tone-quality fixture source videos (large; WAVs are extracted from these — see
# test/fixtures/tone/README.md). Keep the .wav clips, ignore the raw recordings.
test/fixtures/tone/*.MOV
test/fixtures/tone/*.mov
# Intonation-stability calibration clips: ignore raw video + the ffmpeg-converted WAV cache.
test/fixtures/intonation/*.MOV
test/fixtures/intonation/*.mov
test/fixtures/intonation/*.mp4
test/fixtures/intonation/*.m4a
test/fixtures/intonation/*.cal.wav

# Fixture audio itself is large and reproducible/re-recordable — keep only the docs.
test/fixtures/tone/*
!test/fixtures/tone/README.md
test/fixtures/intonation/*
!test/fixtures/intonation/README.md

# Xcode simulator app-data captures (huge, machine-local debugging dumps)
*.xcappdata/

# Raw training frames (14k+ JPGs, ~3.3GB) — too large for git, transfer directly
# to the training host instead (see ml/cloud/RUNPOD.md).
ml/data/raw_frames/
ml/data/raw_frames.tar.gz

# Bulk ML data — never commit
ml/cloud/dataset.tar
ml/cloud/dataset/
ml/cloud/review_flagged.json
ml/cloud/dataset_clean/
