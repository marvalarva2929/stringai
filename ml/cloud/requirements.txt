# torch/torchvision are already installed on GCE's Deep Learning VM image
# (pytorch-latest-gpu, image-project=deeplearning-platform-release) — don't reinstall.
# Pinned: the model's trust_remote_code overrides internal transformers methods
# whose signatures changed in newer releases (e.g. allow_all_kernels kwarg).
transformers==4.57.6
accelerate
gradio>=4.44
pillow
numpy
opencv-python-headless
ultralytics
# nvidia/LocateAnything-3B's trust_remote_code modeling files import these
# directly (verified against the actual source on HF, not guessed) —
# transformers doesn't declare them as deps of its own, so a plain
# `pip install transformers` doesn't pull them in:
#   processing_locateanything.py: decord, lmdb, cv2 (-> opencv-python-headless
#     above), requests
#   modeling_locateanything.py:   peft (LoRA config, imported unconditionally)
# flash_attn is NOT required — modeling_vit.py guards it behind
# is_flash_attn_2_available() and falls back to None if absent.
decord
lmdb
peft
requests
